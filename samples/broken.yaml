name: broken-config
version:1.2.3

server:
	host: localhost
	port: 8080
	tls: no

database:
  host: db.example.com   
  port: 5432
  name: mydb
  name: duplicate-db

title: Hello: World this has a colon
permissions: 0755

tags:
  - web
  - api
